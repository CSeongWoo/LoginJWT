# LoginJWT
260205. 프로젝트 파일 생성 완료. gitignore 설정 완료

## 0. 프로젝트 설정

### SpringSecurity 설정
- CSRF - 저장된 쿠키를 탈취해 위조된 요청을 보내는 공격 행위.
    - JWT에서는 쿠키가 아니라, 브라우저가 토큰을 수동으로 넣어야 하므로 CSRF 공격이 불가능 -> CSRF 설정 OFF
- Authorization - 현재는 로그인페이지 구현이 안되어있으므로 일단 모든 요청을 허용
    - 추후에는 로그인페이지에서만 허용되도록 변경 예정
